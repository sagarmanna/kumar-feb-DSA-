#include<iostream>
using namespace std;
 int main(){

    void DFS (int node, vector <int> & used, vector <int> &)
    code<<node;
    cout<<"\n";
    used[node]=1;
 }

 for(auto u: G[node]){
    parent[u]=node;
    DFS(u,G,used,parent);
 }
}